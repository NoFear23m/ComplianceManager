Public Class uclHighlightedRichtextbox





    'Private Sub _richTextBox_KeyDown_1(sender As Object, e As KeyEventArgs)
    '    Debug.WriteLine(e.Key.ToString & " - " & e.KeyboardDevice.Modifiers.ToString)
    '    If e.Key = Key.Q And e.KeyboardDevice.Modifiers.ToString = "Alt, Control" Then
    '        PopupWindow.IsOpen = True

    '        Dim position As TextPointer = _richTextBox.Selection.[End]

    '        If position Is Nothing Then
    '            Return
    '        End If

    '        Dim positionRect As Rect = position.GetCharacterRect(LogicalDirection.Forward)
    '        PopupWindow.HorizontalOffset = positionRect.X + 25
    '        PopupWindow.VerticalOffset = positionRect.Y + 25
    '    End If

    'End Sub

    'Private Sub _richTextBox_KeyUp(sender As Object, e As KeyEventArgs) Handles _richTextBox.KeyUp
    '    If e.Key = Key.Space Then
    '        PopupWindow.IsOpen = False
    '    End If

    '    If e.Key = Key.Q And e.KeyboardDevice.Modifiers.ToString = "Alt, Control" Then


    '        Dim AtPosition As Integer = InStr(_richTextBox.Text, "@")



    '        '<Run xml:lang="de-at" Background="#FFDCDCDC">@Baumi </Run>
    '    End If

    'End Sub



    'Private Sub uclHighlightedRichtextbox_Unloaded(sender As Object, e As RoutedEventArgs) Handles Me.Unloaded

    'End Sub

    'Private Sub ListBox_MouseDoubleClick(sender As Object, e As MouseButtonEventArgs)
    '    'Dim context As ViewModel.ComplianceItemDetailVM = DataContext




    'End Sub
End Class
