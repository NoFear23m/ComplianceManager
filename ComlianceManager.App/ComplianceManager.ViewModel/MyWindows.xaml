<Controls:MetroWindow x:Class="MyWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:local="clr-namespace:ComplianceManager.ViewModel"
        mc:Ignorable="d"  WindowState="{Binding WindowsPosition.State,Mode=TwoWay}"
         Height="{Binding WindowsSize.Height,Mode =TwoWay}" Width="{Binding WindowsSize.Width,Mode =TwoWay}" Top="{Binding WindowsPosition.Top,Mode =TwoWay}" Left="{Binding WindowsPosition.Left,Mode =TwoWay}"
                      BorderThickness="2" GlowBrush="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Padding="5">
    <Grid>
        
    </Grid>
</Controls:MetroWindow>
